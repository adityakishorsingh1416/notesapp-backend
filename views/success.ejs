<!DOCTYPE html>
<html lang="en">

<head>
    <title>Success</title>
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        @keyframes pop {
            0% {
                transform: scale(0.6);
                opacity: 0;
            }

            100% {
                transform: scale(1);
                opacity: 1;
            }
        }
    </style>
</head>

<body class="bg-gray-900 flex justify-center items-center h-screen">

    <div class="bg-gray-800 p-10 rounded-2xl shadow-2xl text-center w-96 animate-[pop_0.4s_ease-out]">

        <div class="flex justify-center mb-6">
            <div class="w-20 h-20 bg-green-600/20 rounded-full flex items-center justify-center">
                <svg class="w-12 h-12 text-green-500" fill="none" stroke="currentColor" stroke-width="2"
                    viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                </svg>
            </div>
        </div>

        <h1 class="text-white text-3xl font-bold mb-2">Success!</h1>

        <p class="text-gray-400 text-sm mb-6">
            Welcome <span class="text-pink-400 font-semibold">
                <%= username %>
            </span>,
            you have logged in successfully 🎉
        </p>

        <!-- ✅ FIX: Normal link, no JS redirect -->
        <a href="https://notesapp-backend-576p.onrender.com/redirect"
            class="bg-blue-600 px-5 py-2 text-white rounded-lg hover:bg-blue-700 transition">
            Go to Notes App
        </a>


    </div>

</body>

</html>