<!DOCTYPE html>
<html>
<head>
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 flex justify-center items-center h-screen">

    <form action="/login" method="POST" class="bg-gray-800 p-8 rounded-xl w-80">

        <h2 class="text-white text-2xl text-center mb-4">Login</h2>

        <% if (message) { %>
            <p class="text-red-400 mb-2"><%= message %></p>
        <% } %>

        <input name="username" placeholder="Username"
               class="w-full p-2 mb-3 bg-gray-700 text-white rounded">

        <input type="password" name="password" placeholder="Password"
               class="w-full p-2 mb-4 bg-gray-700 text-white rounded">

        <button class="w-full bg-green-500 text-white p-2 rounded">Login</button>

        <p class="text-gray-400 mt-4 text-center text-sm">
            No account? <a href="/register" class="text-blue-400">Register</a>
        </p>
    </form>

</body>
</html>
